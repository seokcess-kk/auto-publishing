# 핵심 HTTP/크롤링
requests>=2.31.0
# requests 2.32.x 의 호환성 체크는 urllib3<2.3, chardet<6 만 허용한다.
# 핀하지 않으면 매 실행마다 RequestsDependencyWarning 노이즈가 출력된다.
urllib3>=2.0,<2.3
chardet>=5.0,<6.0
requests-toolbelt>=1.0.0
beautifulsoup4>=4.12.0
lxml>=5.0.0
feedparser>=6.0.0

# 브라우저 자동화
selenium>=4.18.0
playwright>=1.42.0
browser-cookie3>=0.20.0

# AI
google-generativeai>=0.5.0

# 환경변수
python-dotenv>=1.0.0

# 스케줄러
schedule>=1.2.0

# 네이버 RSA 로그인
rsa>=4.9

# WordPress XML-RPC (선택)
python-wordpress-xmlrpc>=2.3

# 진행 표시
tqdm>=4.66.0

# 엑셀/CSV (선택)
openpyxl>=3.1.0

# Markdown → HTML 변환 (파이프라인 본문 렌더링)
markdown>=3.5

# 카드 이미지 생성 (인스타그램 인포그래픽)
Pillow>=10.0.0
