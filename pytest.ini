[pytest]
testpaths = tests
python_files = test_*.py
python_classes = Test*
python_functions = test_*
addopts =
    -ra
    --strict-markers
    --strict-config
    --tb=short
    -W ignore::DeprecationWarning
markers =
    slow: 외부 네트워크/AI 호출이 포함된 느린 테스트
